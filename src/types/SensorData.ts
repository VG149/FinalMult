// src/types/SensorData.ts
export interface SensorData {
    id: string; // ID único do dado
    date: string; // Data
    hmd: string; // Umidade
    temp: string; // Temperatura
    time: string; // Hora
  }
  